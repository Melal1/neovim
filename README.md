# This is my old neovim config using lazy.nvim , the latest updates are on Java branch .


## I've rewrote the entire config and migrated from lazy.nvim to my own lazy loader with built in vim.pack in the new repo called neovim.
